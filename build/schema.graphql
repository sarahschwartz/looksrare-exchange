type Collection @entity {
  id: ID!
  sales: [Sale!] ! @derivedFrom(field: "collection")
}

type Sale @entity {
  id: ID!
  buyer: String!
  seller: String!
  takerBid: Boolean!
  currency: Bytes!
  collection: Collection!
  tokenID: BigInt!
  amount: BigInt!
  price: BigInt!
  timestamp: BigInt!
}